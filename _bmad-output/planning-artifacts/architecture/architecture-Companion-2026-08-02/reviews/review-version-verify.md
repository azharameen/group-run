Verdict: Mostly reality-checked, but AD-5 is out of date and several stack pins are already behind upstream.

High — AD-5 is wrong as written: current LangGraph docs/code use `astream_events(..., version="v3")` for raw event streaming; `version="v2"` is for `stream()/astream()`'s unified `StreamPart` output, not the sole streaming API. Verified against https://docs.langchain.com/oss/python/langgraph/event-streaming, https://docs.langchain.com/oss/python/langgraph/streaming, and LangGraph source search results in https://github.com/langchain-ai/langgraph.

Medium — The Stack table version lines all exist, but most are stale relative to upstream and should be labeled as intentional pins/legacy ranges if kept: DeepAgents 0.6.8 exists (latest 0.7.1) — https://pypi.org/pypi/deepagents/json; LangGraph 0.6.x exists (latest 1.2.10) — https://pypi.org/pypi/langgraph/json; FastAPI 0.115.x exists (latest 0.141.1) — https://pypi.org/pypi/fastapi/json; LangChain 1.3.x exists (latest 1.3.14) — https://pypi.org/pypi/langchain/json; React 18.3.x exists (latest 19.2.8) — https://registry.npmjs.org/react; Vite 5.4.x exists (latest 8.2.0) — https://registry.npmjs.org/vite; TypeScript 5.5.x exists (latest 7.0.2) — https://registry.npmjs.org/typescript; Tailwind CSS 3.4.x exists (v3 LTS 3.4.19, latest overall 4.3.3) — https://registry.npmjs.org/tailwindcss.

Low — `SqliteSaver` is real (`langgraph.checkpoint.sqlite.SqliteSaver`) and `LANGGRAPH_STRICT_MSGPACK` exists in LangGraph checkpoint serialization, so those decisions were web-confirmed; no correction needed there. Verified against https://docs.langchain.com/oss/python/langgraph/checkpointers and https://github.com/langchain-ai/langgraph.