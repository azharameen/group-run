---
scope: Companion Agentic Organization Platform
purpose: build-substrate
altitude: feature
updated: 2026-08-02T14:31
---

- (event) Architecture run started — coaching path, solo dev, brownfield pivot from FSM patent pipeline to LangGraph supervisor+teams platform
- (decision) Data persistence: Workspace filesystem remains the source of truth for now. Database migration deferred until after LangGraph migration is stable. Agents use CompositeBackend with route-based filesystem access.
- (decision) MCP architecture: User-configurable HTTP MCP servers via UI. Platform-level mcp.json for stdio MCP servers. Future connector framework for Slack, Gmail, Drive, Calendar, Azure DevOps, Firebase, GitHub, Perplexity, etc.
- (decision) Paradigm: LangGraph + DeepAgents is the ONLY orchestration paradigm. Old FSM (transitions library, YAML persistence, custom SSE bus) is dead code — do not import, extend, or reference. Migration is binding, not aspirational.
- (decision) Testing strategy: Comprehensive testing required — unit, integration, E2E, frontend, backend, and agentic testing. Strict test enforcement for AI-assisted development.
- (decision) Dynamic teams/agents: Teams, departments, and agents must be addable/updatable dynamically at runtime. Team definitions loaded from configuration, not hardcoded.
- (decision) Deployment: Docker-based deployments. Virtual env for local testing. Major version upgrades every 6 months, minor/patch updates regularly.
- (decision) Architecture: 3-microservice split — Frontend (React/Vite), Backend API (FastAPI/Express), LangGraph Agents (separate service). Workspace filesystem remains source of truth for now; database migration deferred.
- (decision) Architecture: 2-service split — Frontend (React/Vite) and Backend (FastAPI + LangGraph in-process). LangGraph runtime is a library, not a separate microservice. SQLite/Postgres as database.
- (decision) Database: SQLite for now (via SqliteSaver), migrate to Postgres only when hitting measurable bottlenecks. Sandbox: explicitly deferred — no code execution. Frontend: keep custom SSE + React hooks, no langchain-react or AI SDK.
- (constraint) CRITICAL: astream_events(version='v2') is the ONLY streaming API — there is NO v3 for astream_events. The 'v3 streaming protocol' in LangGraph 1.2.x is a separate internal wire protocol, NOT a replacement for astream_events. Project's ADR-012 referencing v3 is incorrect.
- (constraint) DeepAgents 0.7.x breaking changes: TodoListMiddleware removed from default stack (must opt-in), backend factory pattern removed (concrete instances only), built-in system prompt constants removed, write_file now overwrites (not error-on-exists), new delete tool added (destructive, recursive)
- (constraint) CRITICAL: astream_events(version='v2') is the ONLY streaming API — there is NO v3 for astream_events. The 'v3 streaming protocol' in LangGraph 1.2.x is a separate internal wire protocol, NOT a replacement for astream_events. Project's ADR-012 referencing v3 is incorrect.
- (constraint) DeepAgents 0.7.x breaking changes: TodoListMiddleware removed from default stack (must opt-in), backend factory pattern removed (concrete instances only), built-in system prompt constants removed, write_file now overwrites (not error-on-exists), new delete tool added (destructive, recursive)
- (decision) Team/Agent config: YAML files in config/teams.yaml for definitions (version-controllable, human-readable). Database for runtime state only (active threads, work items).
ation epic — reviewed every 6 months.
- (decision) Version pinning: stick to current working versions (DeepAgents 0.6.8, LangGraph current, React 18, Tailwind 3.4, Vite 5). Major version upgrades deferred to separate migration epic — reviewed every 6 months.
- (decision) Team/Agent config: YAML files in config/teams.yaml for definitions (version-controllable, human-readable). Database for runtime state only (active threads, work items).
- (event) Reviewer gate complete: lint_spine.py (0 findings), version-verify reviewer (AD-5 method name corrected: astream not astream_events for 0.6.x), adversarial reviewer (added AD-13 entity ownership, AD-14 config precedence, AD-15 in-process background work, tightened AD-11 enforcement, AD-12 CI gate, state mutation conventions split)
- (event) spine finalized — 15 ADs, 3 mermaid diagrams, capability map, deferred decisions. Reviewer gate passed with fixes applied.
