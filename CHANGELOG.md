# Changelog

All notable changes to Group Run are documented here.

This file follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/) format and is automatically updated by [release-please](https://github.com/googleapis/release-please) on every merge to `main`.

> **Note for contributors**: Do not edit this file manually. Use [Conventional Commits](https://www.conventionalcommits.org/) — `release-please` reads these and generates changelog entries automatically.

---

## [Unreleased]

### Added
- MkDocs Material documentation site with GitHub Pages deployment
- Interactive Swagger UI integrated with live FastAPI backend
- `llms.txt` auto-generated for AI agent context ingestion
- BMad artifact build hook — copies `_bmad-output/` at build time with zero repo duplication
- CI/CD workflow modernization: pip caching, Playwright browser caching, concurrency groups
- Dependabot auto-updates for Python (pip) and JavaScript (npm) dependencies
- `branches: [main]` filter on production release workflow to prevent false triggers

### Changed
- Migrated database storage from SQLite to PostgreSQL (Docker Compose locally, Supabase in production)
- Replaced SQLite storage with SQLAlchemy 2.0 AsyncEngine, asyncpg, and Alembic schema migrations
- Added reusable GitHub Actions DB migration gate workflow (`db-migrate.yml`)
- Removed all SQLite dependencies, connections, and storage files (Zero-SQLite policy)

### Fixed
- Container startup crash: `teams.yaml` and `instructions/` now bundled inside Docker image
- Port binding updated to `${PORT}` env var for Cloud Run compatibility
- Duplicate `pull_request` trigger removed from `release-beta.yml`

