fastapi>=0.115.6
uvicorn[standard]>=0.52.4
pydantic>=2.13.4
pydantic-settings>=2.7.0
pyyaml>=6.0
# Pinned: 0.6.12 is the version the runtime was developed and verified
# against (create_deep_agent `memory=` file-list semantics). Newer 0.7.x
# releases also expose `memory: list[str]`, but pin exactly so CI
# resolves the same API the backend is tested against.
deepagents==0.7.8
langchain>=0.0.232
langchain-openai>=0.1.0
openai>=0.27.0
httpx>=0.28.0
sse-starlette>=2.2.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
langchain-mcp-adapters>=0.3.0
langgraph-checkpoint-sqlite>=3.1.1
