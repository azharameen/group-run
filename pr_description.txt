Title: 🧪 Add tests for build_agent_backend

🎯 **What:** The testing gap addressed was that `backend/app/agent/backends.py` had no test coverage for the configuration factory `build_agent_backend`.
📊 **Coverage:** The new tests in `backend/tests/test_agent_backends.py` cover the happy path where `CompositeBackend` and associated directories are created, as well as the error condition/fallback where `deepagents` cannot be imported and a `MockBackend` is gracefully returned.
✨ **Result:** Test coverage for `app.agent.backends` has gone from 0% to 100%, and the configuration initialization can be confidently refactored.
